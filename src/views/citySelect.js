import { Grid, Typography } from "@material-ui/core"

export default function CitySelect() {
    return (
        <div>
            <Grid container>
                <Typography variant="h3">
                    City Select Page
                </Typography>
            </Grid>
        </div>
    )
}
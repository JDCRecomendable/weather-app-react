import { Grid, Typography } from "@material-ui/core"

export default function Weather() {
    return (
        <div>
            <Grid container>
                <Typography variant="h3">
                    Weather Page
                </Typography>
            </Grid>
        </div>
    )
}